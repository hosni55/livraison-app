<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        tools:replace="android:allowBackup"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Livraison"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Entry Flow -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Livraison">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.WelcomeActivity" />
        <activity android:name=".activity.RegisterActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activity.LoginActivity" />


        <!-- Controleur Activities -->
        <activity android:name=".activity.controleur.ControleurMainActivity" />
        <activity android:name=".activity.controleur.LivraisonsListActivity" />
        <activity android:name=".activity.controleur.LivraisonDetailActivity" />

        <!-- Livreur Activities -->
        <activity android:name=".activity.livreur.LivreurMainActivity" />
        <activity android:name=".activity.livreur.DeliveryDetailActivity" />
        <activity android:name=".activity.livreur.UpdateDeliveryActivity" />
        <activity android:name=".activity.livreur.CameraProofActivity" />
        <activity android:name=".activity.livreur.MessagesActivity" />
        <activity android:name=".activity.livreur.LiveMapActivity" />
        <activity android:name=".activity.AiAssistantActivity" />

        <activity android:name=".activity.SettingsActivity" />
        <activity android:name=".activity.ProfileActivity" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".util.FcmMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
